# RAG Hallucination in Financial QA — Python dependencies
# Python 3.10+ recommended. Install with: pip install -r requirements.txt
#
# Versions are pinned to known-compatible ranges to keep all 4 members in sync.
# If you must bump a version, do it in a PR so everyone updates together.

# --- Core RAG / orchestration ---
langchain>=0.2,<0.3
langchain-community>=0.2,<0.3
langchain-openai>=0.1,<0.2

# --- Retrieval ---
faiss-cpu>=1.8,<2.0
rank_bm25>=0.2.2,<0.3
sentence-transformers>=3.0,<4.0

# --- Generators / models ---
openai>=1.30,<2.0
transformers>=4.41,<5.0
# Mistral-7B 4-bit quantization (GPU/Colab only; safe to skip on CPU-only machines)
accelerate>=0.30,<1.0
bitsandbytes>=0.43,<1.0 ; platform_system != "Darwin"

# --- Evaluation ---
ragas>=0.1.7,<0.2

# --- Data handling ---
datasets>=2.19,<3.0
pandas>=2.0,<3.0
numpy>=1.26,<2.0
pyarrow>=15.0          # parquet serialization for processed chunks

# --- PDF text extraction (T05) ---
PyMuPDF>=1.24,<2.0
pdfplumber>=0.11,<0.12

# --- Utilities ---
python-dotenv>=1.0,<2.0
tqdm>=4.66,<5.0

# --- Plotting (EDA + paper figures) ---
matplotlib>=3.8,<4.0
seaborn>=0.13,<0.14

# --- Notebooks ---
jupyter>=1.0
ipykernel>=6.29
